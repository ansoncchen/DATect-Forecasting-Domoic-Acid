# Ocean Anomaly Detection — additional deps beyond parent repo
torch>=2.2
zarr>=2.17,<3        # zarr 3.x has Float32 fill-value bug with xarray
shapely>=2.0
matplotlib>=3.8
dask[array]>=2024.1  # required by xarray.open_mfdataset

# Already present in parent requirements.txt (listed for completeness):
# xarray, netcdf4, requests, scikit-learn, tqdm, pandas, numpy, scipy
